# ===========================================
# BACKEND API - ZMIENNE ŚRODOWISKOWE
# ===========================================
# Skopiuj ten plik jako .env i dostosuj wartości do swoich potrzeb
# UWAGA: Plik .env NIE POWINIEN być commitowany do repozytorium!

# -------------------------------------------
# APLIKACJA
# -------------------------------------------
APP_NAME=hackaton
DEBUG=True

# Klucz sekretny dla JWT, sesji itp.
# W PRODUKCJI użyj silnego, losowego klucza!
# Wygeneruj: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production-use-strong-random-key

# -------------------------------------------
# BAZA DANYCH
# -------------------------------------------
# PostgreSQL (docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Alternatywnie SQLite (dla testów lokalnych bez Dockera):
# DATABASE_URL=sqlite:///./app.db

# -------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# -------------------------------------------
# Lista dozwolonych originów (jeśli frontend działa na innym porcie)
# Oddziel przecinkami, bez spacji
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# -------------------------------------------
# SERWER
# -------------------------------------------
# Host i port (jeśli używasz niestandardowych)
# HOST=0.0.0.0
# PORT=8000

# -------------------------------------------
# INNE
# -------------------------------------------
# Poziom logowania (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Timezone
# TZ=Europe/Warsaw
